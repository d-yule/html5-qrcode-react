// import { types } from "mobx-state-tree";
//
// export const ModalState = types.model({
//     open: types.boolean,  // Whether the modal is open or not
//     title: types.maybe(types.string),  // Title of the modal
// }).actions((self) => ({
//     toggle() {
//         self.open = !self.open;  // Toggle the open state of the modal
//     },
//     setTitle(title) {
//         self.title = title;  // Set the title of the modal
//     },
// }));
